machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t osahyoun/rails-demo .


test:
  override:
    #- docker run -d -p 3000:3000 osahyoun/rails-demo; sleep 10
    - docker run osahyoun/rails-demo bundle exec rspec
    #- curl --retry 10 --retry-delay 5 -v http://localhost:3000
    #- docker rspec spec
